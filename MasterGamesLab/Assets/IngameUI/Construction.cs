using Map;
using System.Runtime.Remoting.Messaging;
using UI;
using UnityEngine;

public static class Construction
{
    private static Tile startTile = null;

    public static void StartNewSegment(Tile tile, Edge.EdgeType type)
    {
        if (tile == null) return;

        if (startTile != null)
        {
            
        }

    }


}
